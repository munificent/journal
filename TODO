* Fix post name in disqus code
* Add footer and bottom navigation

* Add RSS link to nav
* Add comments RSS link to nav
* Set up build hook to generate css from scss
* Make sure RSS stuff looks right
* Make separate pygments lexer for old magpie
